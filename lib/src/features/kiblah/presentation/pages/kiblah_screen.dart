import 'package:flutter/material.dart';

class KiblahScreen extends StatelessWidget {
  const KiblahScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [Text("Terakhir dibaca")],
      ),
    );
  }
}
